# F1 Plan Compliance Audit

Verdict: APPROVE

## Must Have checks
- Preserve the existing UI click path: **Satisfied.** The delta diff for this plan contains no `ui/workspaces.rs` changes; it is limited to `hyprline-bar/src/infrastructure/hyprland_ipc.rs` (`/tmp/this-plan-delta.diff:4-8`).
- Make `HyprlandIpc::switch_workspace(id)` inspect the Hyprland socket response instead of write-and-ignore: **Satisfied.** The delta replaces the old write-and-ignore body with `self.send_request(&command)` and response-aware branching (`/tmp/this-plan-delta.diff:40-79`).
- Keep legacy behavior first: **Satisfied.** The delta still sends `dispatch workspace {id}` before any fallback (`/tmp/this-plan-delta.diff:45-46`).
- Treat `ok` (after trimming whitespace) as success and do not send fallback: **Satisfied.** The delta trims the legacy response and returns immediately on `ok` (`/tmp/this-plan-delta.diff:54-57`). The targeted test pass proves the single-command success path (`.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:463-471`).
- Retry exactly once with `dispatch hl.dsp.focus({ workspace = <id> })` when the legacy response is a known Lua-dispatch command error: **Satisfied.** This plan’s delta adds `should_try_lua_workspace_fallback` with the required markers and performs a single fallback to `dispatch hl.dsp.focus({ workspace = {id} })` (`/tmp/this-plan-delta.diff:19-27`, `59-77`). Red-to-green evidence confirms the retry path was added by this plan (`.omo/evidence/task-1-workspace-click-lua-dispatch-fallback.log:3-12`, `.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:463-469`). Live direct-socket QA also shows the expected legacy Lua error and fallback `ok` (`.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:1908-1913`).
- Format the fallback workspace from the typed numeric `i32` id only; do not interpolate label text or user-controlled strings: **Satisfied.** The fallback command is formatted solely from `id` in `format!("dispatch hl.dsp.focus({{ workspace = {} }})", id)` (`/tmp/this-plan-delta.diff:63-64`).
- Do not retry on socket connection/write/read failures; log them and return: **Satisfied.** Both the legacy command path and fallback command path return immediately on `send_request` errors after logging with `eprintln!` (`/tmp/this-plan-delta.diff:46-52`, `64-71`). The transport-failure test passes and emits the expected failure log without panic (`.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:467-471`).
- If both legacy and fallback command responses are not `ok`, log enough context for diagnosis and return without panic: **Satisfied.** The delta logs both command/response values on fallback failure (`/tmp/this-plan-delta.diff:74-79`), and the corresponding test passes (`.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:465-466`).
- Add fake Unix socket tests for legacy success, Lua fallback sequence, double command failure/no panic, and transport failure/no panic: **Satisfied.** The delta adds the fake socket harness, environment guard, and four `workspace_switch_*` tests (`/tmp/this-plan-delta.diff:83-304`, `308-384`). Task 1 shows the expected red failures before the production fallback existed, and Task 2/3 show the tests passing after the implementation landed (`.omo/evidence/task-1-workspace-click-lua-dispatch-fallback.log:3-34`, `.omo/evidence/task-2-workspace-click-lua-dispatch-fallback.log:1-14`, `.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:463-471`).
- Keep all existing workspace label parser tests passing: **Satisfied.** The label-parser work was already present in the baseline target diff; this plan’s delta only adds `workspace_switch_*` tests before the existing `workspace_bind_labels_*` block (`/tmp/this-plan-delta.diff:305-389`). Task 1 shows the existing `workspace_bind_labels_*` tests remained passing while only the new fallback tests were red, and Task 2 shows the full `workspace_` subset passed afterward (`.omo/evidence/task-1-workspace-click-lua-dispatch-fallback.log:14-22`, `.omo/evidence/task-2-workspace-click-lua-dispatch-fallback.log:1-2`, `10-14`).
- Capture live direct-socket QA showing the observed legacy command error and Lua fallback `ok` under the current Hyprland Lua config: **Satisfied.** The evidence log records `legacy_response=...dispatch in lua...syntax might need to be updated` and `fallback_response=ok` for the active workspace id (`.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:1893-1914`).

## Must NOT Have checks
- Must not edit `/home/indiw33d/.config/hypr/hyprland.lua`: **Absent.** The delta diff is limited to the product Rust file; there is no config diff (`/tmp/this-plan-delta.diff:4-8`).
- Must not add `hyprctl`, `Command::new`, `std::process`, shelling out, or any runtime subprocess for workspace switching: **Absent.** The evidence logs report no forbidden runtime-path additions (`.omo/evidence/task-2-workspace-click-lua-dispatch-fallback.log:4`, `.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:1892`).
- Must not add a D-Bus/zbus workspace switching path: **Absent.** The same forbidden-path grep covered `zbus|dbus` and found no matches (`.omo/evidence/task-2-workspace-click-lua-dispatch-fallback.log:4`, `.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:1892`).
- Must not change workspace label parsing, README docs, CSS, tray, Bluetooth, notifications, brightness, settings, or unrelated UI layout: **Absent in this plan’s delta.** The baseline-vs-final delta shows the parser machinery was already present and this plan only added the fallback helper, `switch_workspace` change, and `workspace_switch_*` tests (`/tmp/this-plan-delta.diff:19-27`, `40-79`, `308-384`). The unrelated tracked diff comparison also produced no output, so README/CSS/other dirty files were unchanged by this plan.
- Must not rewrite the workspace click event controller in this plan unless the IPC fallback is implemented and evidence still proves click events do not reach `switch_workspace`: **Absent.** No event-controller or `ui/workspaces.rs` changes appear in the plan delta (`/tmp/this-plan-delta.diff:4-8`).
- Must not change the public `WorkspaceService` trait signature unless tests prove it is unavoidable: **Absent.** The delta changes only the implementation in `hyprland_ipc.rs`; no public trait signature change is present (`/tmp/this-plan-delta.diff:4-8`).
- Must not stage, commit, revert, or overwrite unrelated dirty-worktree changes: **Absent.** The required evidence logs show only test/build/direct-socket QA activity (`.omo/evidence/task-1-workspace-click-lua-dispatch-fallback.log:3-34`, `.omo/evidence/task-2-workspace-click-lua-dispatch-fallback.log:1-15`, `.omo/evidence/task-3-workspace-click-lua-dispatch-fallback.log:1892-1914`), and the unrelated tracked diff comparison was clean.

## Overall reasoning
Re-evaluated against the actual delta introduced by this plan, the earlier rejection basis does not hold. The baseline target diff already contained the workspace label parser changes from a prior plan; this plan’s own delta adds only the Lua-dispatch fallback helper, the response-aware `switch_workspace` implementation, and the new fake-socket `workspace_switch_*` tests.

That delta satisfies every Must Have: legacy-first behavior is preserved, fallback is triggered only on the known Lua-dispatch error signatures, transport and double-failure cases are logged without panic, the new tests went red before implementation and green afterward, existing label-parser tests stayed green, and live direct-socket QA shows legacy failure plus Lua fallback `ok`. It also satisfies every Must NOT Have when judged on this plan’s actual delta. The correct F1 compliance verdict is therefore APPROVE.
